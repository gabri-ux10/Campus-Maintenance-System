apiVersion: v1
kind: Service
metadata:
  name: campusfix-backend
  labels:
    app.kubernetes.io/name: campusfix-backend
spec:
  selector:
    app.kubernetes.io/name: campusfix-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
